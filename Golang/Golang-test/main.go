package main

import "fmt"

func main() {
    fmt.Println("Hello, World!")
	var (
		tableau[] int
		number float32 = 6.2
	)
	tableau = append(tableau, 1,2,3)
	for i := 0; i < len(tableau); i++ {
		println(number)
		println(tableau[i])
	}



}